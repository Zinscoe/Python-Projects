#import random module for random number generation
import random

#then we create a range of values of a dice
min_val = 1
max_val = 6

#then to loop the rolling through user input
roll_again == "yes"

#now to create the loop
while roll_again == "yes" or roll_again == "y":
    print("Rolling the dice...")
    print("The values are: ")

    #now to generate and print the 1st random integer from 1 - 6
    print(random.randomint(min_val,max_val))

    #and generate and print the 2nd random integer from 1 - 6
    print(random.randint(min_val,max_val))

    #now to ask the user if they want to roll the dice again
    roll_again = input("Roll the dices again?")
